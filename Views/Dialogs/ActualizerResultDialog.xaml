<UserControl x:Class="Felweed.Views.Dialogs.ActualizerResultDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Felweed.Views.Dialogs"
             xmlns:dialogs="clr-namespace:Felweed.ViewModels.Dialogs"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=dialogs:ActualizerResultDialogVm, IsDesignTimeCreatable=True}">
    <Grid>
        <ui:TextBox x:Name="ActualizeTextBox" Text="{Binding Text}"
                    IsReadOnly="True"
                    VerticalScrollBarVisibility="Auto"
                    TextChanged="ActualizeTextBox_OnTextChanged"
                    Margin="0, 8, 0, 0"/>
    </Grid>
</UserControl>
