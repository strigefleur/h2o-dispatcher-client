<UserControl x:Class="Felweed.Views.GitlabApiKeyDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Felweed.Views"
             xmlns:vm="clr-namespace:Felweed.ViewModels"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:GitlabApiKeyDialogVm, IsDesignTimeCreatable=True}"
             Width="400"
             d:DesignWidth="400">
    <Grid Margin="8" Width="Auto">
        <StackPanel Margin="8" Width="Auto">
            <ui:TextBox Text="{Binding GitlabApiKey}"
                        x:Name="GitlabApiKeyBox"
                        PlaceholderText="абырвалг"/>
        </StackPanel>
    </Grid>
</UserControl>
